# Dockerfile для MySql
FROM mysql:8.0.32
ENV MYSQL_ROOT_PASSWORD=${SPRING_DATASOURCE_ROOT_PASSWORD} \
    MYSQL_DATABASE=todo \
    MYSQL_USER=${SPRING_DATASOURCE_USERNAME} \
    MYSQL_PASSWORD=${SPRING_DATASOURCE_PASSWORD}
COPY /sql/init.sql /docker-entrypoint-initdb.d
EXPOSE 3306